// <!-- This is a modified version of css.js that has no source map, and written with es6 in mind, because this dang esbuild is throwing errors. -->

import parse from "./lib/parse";
import stringify from "./lib/stringify";

export { parse, stringify };
