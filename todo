
1. layout of the app → v1 ok.





2. create strapi.

→ upload → add images
→ when upload → add the images to the selected plan (add img element) → each element is then selectionable from the preview to move it aroudn
→ save the location of each element, and size and rotation ? we’ll see.
→ 






2. import block
3. move block → reorder  (set attribute through html. use data from attribute)
4. save to db



Active => image in the url?










import image: https://medium.com/@codefoxx/how-to-upload-and-preview-an-image-with-javascript-749b92711b91


then move the image to the strapi server with an id and a couple of data
import image: fetch from strapi.
